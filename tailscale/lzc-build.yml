manifest: ./lzc-manifest.yml
contentdir: ./content
pkgout: ./
icon: ./lzc-icon.png

compose_override:
  services:
    tailscaled:
      devices:
        - /dev/net/tun:/dev/net/tun
      cap_add:
        - NET_ADMIN
        - NET_RAW
